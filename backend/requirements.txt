fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
geoalchemy2==0.15.2
shapely==2.0.6
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
celery==5.4.0
redis==5.0.8
python-multipart==0.0.9
websockets==13.0

# HTTP client — Overpass (OSM road network), ONVIF SOAP, Dubai Pulse open data.
# Deliberately the only network dependency: RTSP is spoken over a raw socket
# so the image needs neither ffmpeg nor OpenCV.
httpx==0.27.2

# --- Optional: real CV stack (M1/M2 in production mode) ------------------
# Not installed by default — torch alone is ~2GB and will not fit alongside a
# dev laptop's other containers. Install in a dedicated GPU worker image:
#   pip install -r requirements-cv.txt
# ultralytics, torch, opencv-python-headless, transformers

# --- Optional: real RAG stack (M5 in production mode) --------------------
# langchain==0.2.16
# langgraph==0.2.20
# faiss-cpu==1.8.0.post1
# mlflow==2.16.0
